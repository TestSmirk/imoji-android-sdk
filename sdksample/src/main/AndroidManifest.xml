<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.imojiapp.imoji.sdksample"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver
            android:name=".ExternalGrantReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.imojiapp.imoji.oauth.external.GRANT"></action>
                <category android:name="com.imojiapp.imoji.category.EXTERNAL_CATEGORY"></category>
            </intent-filter>
        </receiver>
    </application>

</manifest>
